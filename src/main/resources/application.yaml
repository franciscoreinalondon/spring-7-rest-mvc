spring:
  application:
    name: spring-7-rest-mvc
#  profiles:
#    active: localmysql   # Use MySQL profile — application-localmysql.yaml

#  jpa:
#    properties:
#      jakarta:
#        persistence:
#          schema-generation:
#            scripts:
#              action: drop-and-create             # Generate database schema creation scripts
#              create-source: metadata             # Use entity metadata (JPA annotations) as the source for schema generation
#              drop-target: drop-and-create.sql    # File where the generated DROP schema script will be written
#              create-target: drop-and-create.sql  # File where the generated CREATE schema script will be written